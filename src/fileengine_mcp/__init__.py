"""FileEngine MCP server — exposes the FileEngine immutable, versioned
filesystem to AI agents over the Model Context Protocol.

Phase 0: stdio server with LDAP-resolved identity and the first read tools
(``list_directory``, ``read_file``)."""

__version__ = "0.1.0"
